declare namespace Cypress {
  interface Chainable {
    pasteListWithArgument(filenameundefined: string): Chainable;
    checkShootingPhase(weaponundefined: string): Chainable;
    checkFightPhase(weaponundefined: string): Chainable;
  }
}
