import React from "react";

const ChargePhase = (): [React.ReactNode, boolean] => [
  <div className="text-center text-xl font-semibold items-center justify-center align-middle flex flex-col h-full dark:text-gray-200">
    Charged?
  </div>,
  true,
];

export default ChargePhase;
